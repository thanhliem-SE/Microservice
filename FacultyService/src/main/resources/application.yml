server:
  port: 8080

spring:
  application:
    name: FacultyService
  profiles:
    active: default
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:8888